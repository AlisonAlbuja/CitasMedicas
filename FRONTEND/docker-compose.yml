version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80" # Mapear puerto 3000 del host al puerto 80 del contenedor
    restart: always
